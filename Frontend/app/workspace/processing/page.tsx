import { Activity, CheckCircle2, Clock3, Workflow } from 'lucide-react'
import { Shell, SectionEyebrow, EmptyState } from '@/components/cloudspawn-shell'
export default async function ProcessingPage({ searchParams }: { searchParams: Promise<{ job_id?: string }> }) { const params = await searchParams; const jobId = params.job_id; return <Shell><div className="mx-auto max-w-5xl px-5 py-8 md:px-8 md:py-12"><SectionEyebrow>System</SectionEyebrow><h1 className="text-3xl font-semibold tracking-tight">Processing</h1><p className="mt-2 text-muted-foreground">Monitor document processing jobs as they run.</p>{jobId ? <div className="mt-8 grid gap-4 lg:grid-cols-[1.2fr_0.8fr]"><div className="rounded-xl border bg-card p-6"><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">Building knowledge base</p><h2 className="mt-1 text-xl font-medium">Processing documents</h2></div><Activity className="size-5 text-muted-foreground" /></div><div className="mt-8 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full w-1/3 rounded-full bg-foreground" /></div><p className="mt-3 text-sm text-muted-foreground">Waiting for job updates from the backend.</p><div className="mt-8 grid gap-3 sm:grid-cols-2"><div className="rounded-lg border p-4"><p className="text-xs text-muted-foreground">Job ID</p><p className="mt-2 truncate font-mono text-sm">{jobId}</p></div><div className="rounded-lg border p-4"><p className="text-xs text-muted-foreground">Status</p><p className="mt-2 flex items-center gap-2 text-sm"><span className="size-2 rounded-full bg-amber-500" />Processing</p></div></div></div><div className="rounded-xl border bg-card p-6"><p className="text-xs font-medium uppercase tracking-[0.18em] text-muted-foreground">Execution workers</p><p className="mt-2 text-sm leading-6 text-muted-foreground">Worker details will appear here when the runtime exposes them.</p><div className="mt-6 flex flex-col gap-3"><Worker label="Orchestrator" icon={Workflow} /><Worker label="Document tasks" icon={Clock3} /><Worker label="Retrieval index" icon={CheckCircle2} /></div></div></div> : <div className="mt-8"><EmptyState icon={Activity} title="No active jobs" description="Start by uploading documents and building a knowledge base." /></div>}</div></Shell> }
function Worker({ label, icon: Icon }: { label: string; icon: React.ElementType }) { return <div className="flex items-center gap-3 rounded-lg border px-3 py-3"><Icon className="size-4 text-muted-foreground" /><span className="flex-1 text-sm">{label}</span><span className="text-xs text-muted-foreground">Standby</span></div> }
